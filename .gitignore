# OpenCodeBox session data (never commit)
.opencode-data/
project-data/

# ProjectTXT artifacts (never commit)
ProjectTXT
ProjectTXT.txt

# OpenCodeBox shared skills/commands (symlinks; never commit)
.opencode/skills
.opencode/command
